# Core — always required
opencv-python-headless>=4.9
numpy>=1.26
scipy>=1.12

# Face detection (YOLOv8 — best accuracy, ~60 MB download)
ultralytics>=8.2

# Active speaker detection model runtime
torch>=2.2
torchvision>=0.17
python-speech-features>=0.6   # MFCC extraction for LR-ASD audio stream

# HTTP server (optional but recommended over stdlib fallback)
fastapi>=0.110
uvicorn[standard]>=0.29

# MediaPipe face detection (optional fallback if YOLO unavailable)
mediapipe>=0.10
